<?xml version="1.0" encoding="utf-8"?>
<!--
  Ripple behind the bookmark star inside the URL chip. The toolbar's page
  actions get no background of their own, so without this the star has no press
  feedback at all — it reads as a decoration rather than a button.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="#FFFFFFFF" />
        </shape>
    </item>
</ripple>
