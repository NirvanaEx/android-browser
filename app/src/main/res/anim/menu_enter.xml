<?xml version="1.0" encoding="utf-8"?>
<!--
  The app menu, growing out of the ⋮ it was opened from. Pivot at the top right
  corner because that is where the button is — a menu that expands from the
  thing you pressed is the platform's own idiom for "this belongs to that".
-->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:shareInterpolator="false">

    <alpha
        android:fromAlpha="0.0"
        android:toAlpha="1.0"
        android:duration="120"
        android:interpolator="@android:interpolator/decelerate_quint" />

    <scale
        android:fromXScale="0.85"
        android:fromYScale="0.85"
        android:toXScale="1.0"
        android:toYScale="1.0"
        android:pivotX="100%"
        android:pivotY="0%"
        android:duration="180"
        android:interpolator="@android:interpolator/decelerate_quint" />
</set>
