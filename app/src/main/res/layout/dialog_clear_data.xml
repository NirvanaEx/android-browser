<?xml version="1.0" encoding="utf-8"?>
<!--
  Body of the "clear browsing data" dialog: a period, then what to delete.

  Both groups are filled in code from BrowsingDataCleaner's own enums, so the
  list of periods and the list of data types exist in exactly one place. This
  file only decides how they are laid out.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="4dp">

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/periodGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:singleSelection="true"
        app:selectionRequired="true"
        app:chipSpacingVertical="2dp" />

    <LinearLayout
        android:id="@+id/typeGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="vertical" />

    <TextView
        android:id="@+id/clearNote"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:textSize="12sp"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:text="@string/clear_period_note" />
</LinearLayout>
