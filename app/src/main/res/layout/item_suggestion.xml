<?xml version="1.0" encoding="utf-8"?>
<!--
  One omnibar suggestion: kind icon in a round holder, title, URL underneath,
  and a trailing arrow that puts the text into the address bar instead of
  loading it.

  56dp rows with 8dp side padding inside a panel that is itself inset — the
  previous 52dp row with 18dp padding was sized for a full-width list and left
  the text almost touching the panel edge.

  Ids are bound in SuggestionAdapter.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="56dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:paddingStart="10dp"
    android:paddingEnd="4dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <com.upgrid.browser.ui.SiteIconView
        android:id="@+id/suggestionIcon"
        android:layout_width="36dp"
        android:layout_height="36dp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/suggestionTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="15sp"
            android:textColor="?attr/colorOnSurface"
            tools:text="Upgrid Browser — GitHub" />

        <TextView
            android:id="@+id/suggestionUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="12sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            tools:text="github.com/NirvanaEx/android-browser" />
    </LinearLayout>

    <!-- Saved-page marker. The row's icon is the site's own now, so "this one
         is a bookmark" needs saying somewhere else. -->
    <ImageView
        android:id="@+id/suggestionBadge"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginStart="6dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_bookmark_filled"
        android:visibility="gone"
        app:tint="?attr/colorPrimary"
        tools:visibility="visible" />

    <ImageButton
        android:id="@+id/suggestionFill"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="0dp"
        android:scaleType="center"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/suggestion_fill"
        android:src="@drawable/ic_arrow_upleft"
        android:tint="?attr/colorOnSurfaceVariant" />
</LinearLayout>
