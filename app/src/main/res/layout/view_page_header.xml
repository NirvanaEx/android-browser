<?xml version="1.0" encoding="utf-8"?>
<!--
  Header shared by the History, Bookmarks and Tabs screens: back arrow, title,
  and one optional action on the right.

  These are full screens now rather than bottom sheets, so they need a real way
  back rather than a drag handle. Included (not copy-pasted) so the three stay
  aligned to the pixel; ViewBinding exposes it as `binding.header.*`.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="4dp"
    android:paddingEnd="8dp"
    android:background="?attr/colorSurface">

    <ImageButton
        android:id="@+id/btnBack"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:padding="0dp"
        android:scaleType="center"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/nav_back"
        android:src="@drawable/ic_arrow_back"
        app:tint="?attr/colorOnSurface" />

    <TextView
        android:id="@+id/pageTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textAppearance="?attr/textAppearanceTitleLarge"
        android:textColor="?attr/colorOnSurface"
        tools:text="История" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnHeaderAction"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="0dp"
        android:visibility="gone"
        tools:visibility="visible"
        tools:text="Очистить" />
</LinearLayout>
