<?xml version="1.0" encoding="utf-8"?>
<!--
  Translation bar, in the same slot as find-in-page.

  Modelled on Chrome's: two chips, "original" and the target language, with the
  active one filled. That is the whole control — the source language is
  detected and shown as a caption, not as a second picker, because choosing it
  by hand is a thing nobody does and it was half of what made Google's own
  banner (the one this replaces) so bulky.

  Ids are bound by TranslateController.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="?attr/colorSurfaceContainer"
    android:paddingStart="14dp"
    android:paddingEnd="4dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <ImageView
        android:id="@+id/translateIcon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_translate"
        android:tint="?attr/colorPrimary" />

    <ProgressBar
        android:id="@+id/translateSpinner"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:visibility="gone"
        android:indeterminateTint="?attr/colorPrimary" />

    <TextView
        android:id="@+id/translateStatus"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="13sp"
        android:textColor="?attr/colorOnSurfaceVariant"
        tools:text="Английский" />

    <TextView
        android:id="@+id/translateChipOriginal"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:gravity="center"
        android:background="@drawable/bg_chip_toggle"
        android:clickable="true"
        android:focusable="true"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:textSize="13sp"
        android:textColor="@color/chip_toggle_text"
        android:text="@string/translate_chip_original" />

    <TextView
        android:id="@+id/translateChipTarget"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:layout_marginStart="6dp"
        android:gravity="center"
        android:background="@drawable/bg_chip_toggle"
        android:clickable="true"
        android:focusable="true"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:textSize="13sp"
        android:textColor="@color/chip_toggle_text"
        tools:text="Русский" />

    <ImageButton
        android:id="@+id/translateClose"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="2dp"
        android:padding="0dp"
        android:scaleType="center"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/find_close"
        android:src="@drawable/ic_close"
        android:tint="?attr/colorOnSurfaceVariant" />
</LinearLayout>
