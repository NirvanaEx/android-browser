<?xml version="1.0" encoding="utf-8"?>
<!--
  Filter field shared by the History and Bookmarks screens.

  A plain EditText on a rounded fill rather than a Material TextInputLayout: the
  latter brings a floating label, a focus animation and ~20dp of vertical chrome
  for what is one line of filter text.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="14dp"
    android:paddingEnd="8dp"
    android:background="@drawable/bg_sheet_search">

    <ImageView
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_find"
        app:tint="?attr/colorOnSurfaceVariant" />

    <EditText
        android:id="@+id/searchInput"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:background="@null"
        android:textColorHint="?attr/colorOnSurfaceVariant"
        android:textColor="?attr/colorOnSurface"
        android:textSize="14sp"
        android:inputType="text"
        android:imeOptions="actionSearch"
        android:maxLines="1"
        android:singleLine="true"
        android:importantForAutofill="no" />
</LinearLayout>
